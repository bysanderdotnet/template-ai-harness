{
  "$comment": "Harness config, owned by the harness CLI — do not hand-edit. 'setup' tracks guided first-time setup (`./agents.sh setup`) and is removed when setup completes. 'commands' is the registry managed by `./agents.sh cmd set/rm`: verify = definition of done step (run by `verify`, in listed order, cheap/fast first); init = session-start smoke check.",
  "commands": {
    "wrapper-syntax": {
      "run": "sh -n agents.sh",
      "verify": true
    },
    "python-syntax": {
      "run": "python3 -m py_compile .agents/agents.py",
      "verify": true
    },
    "harness-check": {
      "run": "./agents.sh check",
      "verify": true
    }
  }
}
