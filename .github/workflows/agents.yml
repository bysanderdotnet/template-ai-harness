# Human-owned: AI agents must never edit this file.
# When the project needs a toolchain before ./AGENTS.sh ci can run the
# registered commands (e.g. actions/setup-node + npm ci), a human adds
# those steps below.
name: agents

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: read

# Superseded verify runs on the same branch/PR are wasted minutes; cancel them.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'schedule' }}

jobs:
  verify:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 30 # raise if builds are slow
    steps:
      - uses: actions/checkout@v4

      - name: Harness gates
        # check always; init + verify once guided setup is complete.
        run: ./AGENTS.sh ci

  auto-merge-pr:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      checks: read
      statuses: read
    outputs:
      has_open_prs: ${{ steps.merge.outputs.has_open_prs }}
    steps:
      - uses: actions/checkout@v4

      - name: Auto-merge eligible PRs
        id: merge
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./AGENTS.sh automate auto-merge-pr --repo "${{ github.repository }}"

  auto-create-pr:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: auto-merge-pr
    steps:
      - uses: actions/checkout@v4

      - name: Trigger next PR webhook
        run: >-
          ./AGENTS.sh automate auto-create-pr
          --has-open-prs "${{ needs.auto-merge-pr.outputs.has_open_prs || 'true' }}"
